export const config = {
  prefix: "!",
  admins: [] as string[],
  messages: {
    adminOnly: "This command is restricted to administrators.",
    groupOnly: "This command can only be used in groups.",
    privateOnly: "This command can only be used in private chats.",
    cooldown: "Please wait before using this command again.",
  },
};
